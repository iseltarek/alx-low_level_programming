#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print all alphabet letters
 *
 * Return: Always 0 (success)
*/

int main(void)
{
	for (char i = 'a'; i <= 'z'; i++)
	putchar(i);
	putchar('\n');
	return (0);
}
