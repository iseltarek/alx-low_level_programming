#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print alphabet using putchar function. 
 *
 * Return: Always 0 (success)
*/

int main(0) 
{
	for (char i = 'a'; i <= 'z'; i++)
	putchar(i);
	return (0);
}
